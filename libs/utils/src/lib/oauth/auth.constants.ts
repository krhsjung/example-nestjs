export enum AuthProvider {
  GOOGLE = 'google',
  APPLE = 'apple',
}

export enum AuthFlow {
  REDIRECT = 'redirect',
  POPUP = 'popup',
  ANDROID = 'android',
  IOS = 'ios',
}

export enum OAuthMethod {
  LIBRARY = 'library',
  DIRECT = 'direct',
}
