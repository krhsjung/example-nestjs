export enum AuthProvider {
  EMAIL = 'email',
  GOOGLE = 'google',
  APPLE = 'apple',
}

export enum AuthFlow {
  REDIRECT = 'redirect',
  POPUP = 'popup',
  ANDROID = 'android',
  IOS = 'ios',
}

export enum OAuthMethod {
  LIBRARY = 'library',
  DIRECT = 'direct',
}
